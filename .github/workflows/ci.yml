name: PitchPilot CI/CD
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: "20"

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
      - run: npm ci
      - name: TypeScript Check
        run: npx tsc --noEmit
      - name: ESLint
        run: npx next lint --dir src
        continue-on-error: true

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: lint-and-typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
      - run: npm ci
      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}
      - name: Check bundle size
        run: |
          echo "Build output:"
          ls -lah .next/static/chunks/ | head -20
          echo ""
          echo "Total .next size:"
          du -sh .next/

  security-scan:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
      - run: npm ci
      - name: NPM Audit
        run: npm audit --production --audit-level=high
        continue-on-error: true
      - name: Check for secrets in code
        run: |
          echo "Scanning for hardcoded secrets..."
          FOUND=0
          # Check for live API keys
          if grep -rn 'sk_live_\|sk_test_\|gsk_[a-zA-Z0-9]\{20\}\|re_[a-zA-Z0-9]\{20\}\|AIzaSy[a-zA-Z0-9_-]\{30\}' --include="*.ts" --include="*.tsx" src/ 2>/dev/null; then
            echo "::error::Hardcoded API keys found!"
            FOUND=1
          fi
          # Check for hardcoded Bearer tokens
          if grep -rn '"Bearer [a-zA-Z0-9]\{20\}"' --include="*.ts" --include="*.tsx" src/ 2>/dev/null; then
            echo "::error::Hardcoded Bearer tokens found!"
            FOUND=1
          fi
          if [ "$FOUND" -eq 1 ]; then
            exit 1
          fi
          echo "No hardcoded secrets found."

  # Deploy to Vercel (only on main branch push)
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    needs: [build, security-scan]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: "--prod"
